function trueOrFalse(wasThatTrue) {
    // Only change code below this line
    let str = '';
    (wasThatTrue) ?
        str = "Yes, that was true" : str = "No, that was false";
    return str;

    // Only change code above this line

}